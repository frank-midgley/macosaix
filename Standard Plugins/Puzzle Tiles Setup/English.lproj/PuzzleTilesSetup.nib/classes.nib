{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                cancelOtherSize = id; 
                setCurviness = id; 
                setImagesAligned = id; 
                setOtherSize = id; 
                setTabbedSides = id; 
                setTilesAcross = id; 
                setTilesCount = id; 
                setTilesDown = id; 
                setTilesSize = id; 
                setTilesSizeType = id; 
            }; 
            CLASS = MacOSaiXPuzzleTileShapesEditor; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                alignImagesMatrix = NSMatrix; 
                curvinessSlider = NSSlider; 
                curvinessTextField = NSTextField; 
                editorView = NSView; 
                okButton = NSButton; 
                otherSizeField = NSTextField; 
                otherSizePanel = NSPanel; 
                tabbedSidesSlider = NSSlider; 
                tabbedSidesTextField = NSTextField; 
                tilesAcrossSlider = NSSlider; 
                tilesAcrossStepper = NSStepper; 
                tilesAcrossTextField = NSTextField; 
                tilesCountSlider = NSSlider; 
                tilesDownSlider = NSSlider; 
                tilesDownStepper = NSStepper; 
                tilesDownTextField = NSTextField; 
                tilesSizeMatrix = NSMatrix; 
                tilesSizePopUp = NSPopUpButton; 
                tilesSizeSlider = NSSlider; 
                tilesSizeTabView = NSTabView; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}
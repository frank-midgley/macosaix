{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {setUnits = id; }; 
            CLASS = MacOSaiXPDFExportEditor; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                editorView = NSView; 
                heightField = NSTextField; 
                unitsPopUp = NSPopUpButton; 
                widthField = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}
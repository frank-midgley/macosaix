{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {setScreenType = id; }; 
            CLASS = MacOSaiXScreenSetupController; 
            LANGUAGE = ObjC; 
            OUTLETS = {screenTypeMatrix = NSMatrix; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}
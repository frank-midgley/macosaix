{
    IBClasses = (
        {CLASS = MacOSaiXEditorsView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = MacOSaiXEventCatchingPanel; LANGUAGE = ObjC; SUPERCLASS = NSWindow; }, 
        {CLASS = MacOSaiXPopUpButton; LANGUAGE = ObjC; SUPERCLASS = NSButton; }, 
        {CLASS = MacOSaiXSplitView; LANGUAGE = ObjC; SUPERCLASS = NSSplitView; }, 
        {
            CLASS = MacOSaiXTextFieldCell; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSTextFieldCell; 
        }, 
        {
            ACTIONS = {
                saveMosaicAs = id; 
                setBlend = id; 
                setMaximumZoom = id; 
                setMinimumZoom = id; 
                setZoom = id; 
                togglePause = id; 
                viewFullScreen = id; 
            }; 
            CLASS = MacOSaiXWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                blendSlider = NSSlider; 
                editingContentView = NSView; 
                editingMosaicScrollView = NSScrollView; 
                editingSplitView = NSSplitView; 
                editingStatusViewBox = NSBox; 
                editingView = NSView; 
                editorsView = MacOSaiXEditorsView; 
                imagesFoundField = NSTextField; 
                minimalContentView = NSView; 
                minimalMosaicScrollView = NSScrollView; 
                minimalStatusViewBox = NSBox; 
                mosaicView = MosaicView; 
                pauseButton = NSButton; 
                statusField = NSTextField; 
                statusView = NSView; 
                zoomSlider = NSSlider; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = MosaicView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = NSWindowController; LANGUAGE = ObjC; SUPERCLASS = NSResponder; }
    ); 
    IBVersion = 1; 
}
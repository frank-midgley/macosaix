{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                cancelExport = id; 
                setBackground = id; 
                setCreateWebPage = id; 
                setFade = id; 
                setImageFormat = id; 
                setIncludeOriginalImage = id; 
                setOpenImageWhenComplete = id; 
                setResolution = id; 
                setShowNonUniqueMatches = id; 
                setUnits = id; 
            }; 
            CLASS = MacOSaiXExportController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                accessoryView = NSView; 
                backgroundPopUp = NSPopUpButton; 
                createWebPageButton = NSButton; 
                delegate = id; 
                fadeSlider = NSSlider; 
                formatMatrix = NSMatrix; 
                heightField = NSTextField; 
                includeOriginalButton = NSButton; 
                mosaicView = MosaicView; 
                openWhenCompleteButton = NSButton; 
                resolutionPopUp = NSPopUpButton; 
                showNonUniqueMatchesButton = NSButton; 
                unitsPopUp = NSPopUpButton; 
                widthField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = MosaicView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}
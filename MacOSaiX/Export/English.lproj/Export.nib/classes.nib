{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelExport = id; setFormat = id; setOpenWhenCompleted = id; }; 
            CLASS = MacOSaiXExportController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                delegate = id; 
                exportSettings = id; 
                exporterEditor = id; 
                formatPopUp = NSPopUpButton; 
                openWhenCompletedButton = NSButton; 
                separatorLine = NSBox; 
                sharedView = NSView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = MosaicView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}